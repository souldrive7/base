# バックアップ救出状況の確定（2026-07-02 調査）

6/30 に旧PCで実行したバックアップ（C:\Users\sould\GoogleDrive\ への退避）の到達状況を、
Drive API 検索＋スマホのDriveアプリ画面（パソコンセクション）で照合した結果。

## 結論

**6/30 のバックアップ6フォルダは、クラウドに到達していない。**
マイドライブに「PCバックアップ_2026-06-30」が空フォルダとして存在し（アップロード開始直後の状態）、
「パソコン」セクションにも *_Backup フォルダは無い。ローカルコピー完了（✅）後、
クラウド同期が完了する前にPCがリセットされたと判断される。

## 分類表

### ① クラウドに実在（救出可能）
| 対象 | 場所 |
|---|---|
| デスクトップ / ドキュメント / 画像 / Downloads（旧PCの同期分） | Drive「パソコン」→「マイ ノートパソコン」※中身の最新日付要確認 |
| OneDrive - 滋賀大学 | Drive「パソコン」内 ＋ 大学 Microsoft 365 クラウド本体（PCと無関係に無事） |
| 修論バックアップ | マイドライブ `thesis_backup_20260327` |
| JSAI 関連PDF（JSAI2017〜2025 各論文） | マイドライブ内に複数保存済み（2025年に別途保存されたもの） |
| WSL 初期化スクリプト | `init_wsl_workspace.ps1`（2026-04-20版） |
| Colab ノートブック・講義資料・インストーラー類 | マイドライブ |

### ② GitHub側で無事（要スマホ確認）
- msc-thesis / Kaggle / stock_analysis の各リポジトリ（最終pushまでの内容）

### ③ 消失濃厚（Windows.old が唯一の望み）
- AppData\Roaming（25GB）、Videos
- WSL Ubuntu-24.04.tar / Ubuntu-22.04.tar
- WiFiパスワード・アプリ一覧（System_Backup）
- .claude メモリ（claude_backup）
- git管理外フォルダ（Dev_Backup。ただし JSAI_report のPDF群は①で別途存在）

## Windows.old 調査結果（2026-07-03 実施・完了）

- Windows.old 作成日時: **2026/07/02 23:50:14**（自動削除期限は約10日後＝7/12頃。ただし調査完了のため以後の作業には無関係）
- `C:\Windows.old\Users\Akifumi Goto\` の中身を確認した結果：
  - Desktop: 空
  - Documents: 既定ジャンクション（My Music/My Pictures/My Videos）のみ、実ファイルなし
  - AppData\Roaming: `Microsoft` フォルダのみ。アプリの設定・データは一切なし
  - AppData\Local\Packages: Ubuntu/WSLディストリなし
  - 全項目のタイムスタンプが 2026/07/02 22:45〜23:52 の1時間に集中（NTUSER.DATも1MBの初期値）

**→ この Windows.old は、リセット処理中に一時的に作られた空の新規プロファイルであり、
6/30 にバックアップした本物の旧環境（25GBのRoaming・WSL・.claudeメモリ等）は含まれていない。
リセット時に「すべて削除する」を選択したとみられ、この経路からの救出は不可能と確定。**

これ以上 Windows.old を探索する意味はない。以後は下記「代替復元」に一本化する。

## ③（消失濃厚 → 消失確定）の代替復元

- **WSL**: `wsl --install -d Ubuntu-24.04` で新規作成 → `init_wsl_workspace.ps1` で作業環境を初期化
- **WiFiパスワード**: 主要な回線はルーター本体のラベル／スマホの保存済みネットワークから再登録
- **アプリ一覧**: 本リポジトリの `setup/install-apps.ps1`＋`restore-phase2-20260615.ps1` が代替（クラウド記録から再構成済み）
- **.claude メモリ**: 再構築。今後は `%USERPROFILE%\.claude` を定期バックアップ対象に追加
- **AppData\Roaming**: アプリ再インストールで大半は再生成される。個別データ（ブラウザプロファイル等）はクラウド同期分で復旧
